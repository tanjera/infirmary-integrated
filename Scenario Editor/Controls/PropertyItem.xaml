<UserControl x:Class="ScenarioEditor.PropertyItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ScenarioEditor"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch" Margin="2">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Name="lblKey" Grid.Column="0" Grid.Row="0"
               HorizontalAlignment="Left" Height="Auto"
               Margin="2" />
        <TextBox Name="txtValue" Grid.Column="1" Grid.Row="0"
                 HorizontalAlignment="Stretch" Height="Auto"
                 VerticalContentAlignment="Center"
                 Margin="2" />
    </Grid>
</UserControl>